# Source Directory

`src/main.js` is the current working plugin source.

`src/styles.css` is the current working stylesheet source.

The surrounding folders are the target modular architecture. They are intentionally scaffolded but not yet populated because the current priority is preserving a known-working plugin before extracting modules.
