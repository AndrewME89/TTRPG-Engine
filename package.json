{
  "name": "ttrpg-engine",
  "version": "2.1.0",
  "description": "Full TTRPG campaign engine for Obsidian: campaign management, world-building, NPCs, encounters, quests, sessions, secrets, compendium, homebrew, tile maps, and player-facing mode.",
  "main": "main.js",
  "author": "Andrew Edwards",
  "license": "MIT",
  "private": false,
  "scripts": {
    "check": "node --check src/main.js && node scripts/validate-manifest.mjs && node scripts/check-release-files.mjs",
    "build": "node scripts/build.mjs && npm run check",
    "package": "npm run build && node scripts/package-release.mjs",
    "clean": "node scripts/clean.mjs"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "ttrpg",
    "dnd",
    "campaign-management",
    "tile-map"
  ],
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {}
}
